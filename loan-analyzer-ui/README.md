# loan-analyzer-ui (Solid.js)

Simple Solid.js frontend for the **loan-analyzer** backend.

## Requirements

- Node.js 18+
- Backend running locally on `http://localhost:8090`

## Run

```bash
cd loan-analyzer-ui
npm install
npm run dev
```

Open `http://localhost:5173`